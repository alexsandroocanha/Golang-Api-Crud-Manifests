apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: golang-api-crud-default-deny
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: golang-api-crud-allow-traffic
spec:
  podSelector:
    matchLabels:
      app: golang-api-crud
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # - from:
    #     - namespaceSelector:
    #         matchLabels:
    #           kubernetes.io/metadata.name: ingress-nginx
    #   ports:
    #     - protocol: TCP
    #       port: 8000
    - {}
  egress:
    # - to:
    #     - namespaceSelector: {}
    #   ports:
    #     - protocol: UDP
    #       port: 53
    #     - protocol: TCP
    #       port: 53
    # - to:
    #     - namespaceSelector: {}
    #   ports:
    #     - protocol: TCP
    #       port: 5432
    - {}