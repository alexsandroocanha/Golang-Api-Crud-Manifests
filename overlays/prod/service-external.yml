# apiVersion: v1
# kind: Service
# metadata:
#   name: golang-api-crud-external
# spec:
#   type: LoadBalancer
#   selector:
#     app: golang-api-crud
#   ports:
#     - name: http
#       port: 8082
#       targetPort: http
#       protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: golang-api-crud-external
spec:
  type: NodePort
  selector:
    app: golang-api-crud
  ports:
    - name: http
      port: 8082
      targetPort: http
      nodePort: 30082
      protocol: TCP